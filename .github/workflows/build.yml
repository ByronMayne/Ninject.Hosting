name: Build

on: [push]

env:
  solutionPath: ./src/Ninject.Hosting.sln
  configuration: Release

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: CheckOut
      uses: ByronMayne/Shared.Actions/gitversion_checkout@ccbffcf5ee71b9183de3112bb1e8e780753d1efc
    - name: Print STuff
      run: 'echo "HI: ${{ env.assemblySemVer }}"'
    - name: Build 
      uses: ByronMayne/Shared.Actions/build_csharp@ccbffcf5ee71b9183de3112bb1e8e780753d1efc
      with: 
        publish: false 
        configuration: ${{ env.configuration }}
        solutionOrProjectPath: ${{ env.solutionPath }}

